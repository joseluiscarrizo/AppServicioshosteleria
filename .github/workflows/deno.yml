# Workflow disabled: this is a Node.js/React project, not a Deno project.
# deno lint was incorrectly running against JS/TS source files causing 76 false-positive errors.
# The project CI is handled by .github/workflows/ci.yml

name: Deno (disabled)

on:
  workflow_dispatch: # only runs if manually triggered — effectively disabled