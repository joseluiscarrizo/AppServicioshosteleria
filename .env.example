# Base44 BaaS Configuration
# Get these values from the Base44 Dashboard → Settings → API Keys
VITE_BASE44_APP_ID=your_base44_app_id
VITE_BASE44_BACKEND_URL=https://your-backend.base44.app

# Optional: enable legacy SDK import paths (set to true only if needed)
# BASE44_LEGACY_SDK_IMPORTS=false
