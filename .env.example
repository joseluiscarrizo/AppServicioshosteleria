# ==============================================
# Camareros Bcn — Variables de Entorno
# ==============================================
# Copia este archivo como .env y completa los valores:
#   cp .env.example .env
#
# IMPORTANTE: Nunca subas el archivo .env a git.
# Las variables VITE_* son expuestas al navegador (frontend).
# ==============================================

# ── Base44 BaaS (REQUERIDAS para arrancar la app) ──
VITE_BASE44_APP_ID=your_base44_app_id_here
# URL del backend de Base44. Usa https://api.base44.com para producción/directo.
# En entornos Base44 Preview (modal.host) este valor se inyecta automáticamente.
# Para desarrollo local con proxy propio, apunta aquí a tu proxy: http://localhost:3001
VITE_BASE44_BACKEND_URL=https://api.base44.com

# ── Base44 Proxy (opcional en desarrollo) ──────────
# Cuando está definida, el plugin de Vite crea un proxy local:
#   /api  →  VITE_BASE44_APP_BASE_URL
# Útil para evitar problemas de CORS en desarrollo local.
# Valor típico: la misma URL que VITE_BASE44_BACKEND_URL.
# VITE_BASE44_APP_BASE_URL=https://api.base44.com

# ── Entorno ────────────────────────────────────────
NODE_ENV=development

# ── WhatsApp Business API (opcional) ───────────────
# WHATSAPP_API_TOKEN=your_whatsapp_bearer_token
# WHATSAPP_PHONE_NUMBER=your_phone_number_id  # e.g. 1234567890 (used by enviarWhatsAppDirecto)

# ── Gmail SMTP (opcional) ──────────────────────────
# GMAIL_USER=tu_email@gmail.com
# GMAIL_PASS=tu_app_password_de_16_caracteres

# ── Google Sheets API (opcional) ───────────────────
# GOOGLE_SERVICE_ACCOUNT_EMAIL=service-account@proyecto.iam.gserviceaccount.com
# GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
# GOOGLE_SPREADSHEET_ID=your_spreadsheet_id